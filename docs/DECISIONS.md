# DECISIONS

Формат: дата — решение — потому что причина.

- 2026-09-05 — Источник истины по логике MVP: `Convoy_mind_diagramm.pdf`; docx — для UX-формулировок и целевого экрана — потому что PDF конкретнее и новее.
- 2026-09-05 — Пользователь MVP: водитель, телефон, портрет — потому что так сказал владелец продукта; диспетчерский сценарий откладываем.
- 2026-09-05 — Роутинг: TomTom Orbis Routing v2 `calculateRoute` (POST) — потому что v1 в deprecation, а Orbis умеет `legs[].routeStop.pauseDurationInSeconds`, что напрямую моделирует перерывы. Ограничение: из грузовых параметров только `vehicleWeightInKilograms`, `travelMode` только `car` (см. NOT_NOW).
- 2026-09-05 — Трафик: не отдельный вызов Traffic API, а `traffic: 'live'` в том же routing-запросе — потому что один вызов закрывает два блока диаграммы.
- 2026-09-05 — Парковки: собственная схема + MSW-мок с seeded-распределением — потому что бесплатного общеевропейского API занятости нет. Немецкий открытый API Autobahn GmbH (`/services/parking_lorry`) — кандидат на реальные данные позже.
- 2026-09-05 — «Бекенд» = чистый TS-модуль `src/core` внутри Expo-проекта, без сервера — потому что для MVP сервер не нужен, а два процесса усложняют старт. Ключ TomTom в `.env` приложения (см. NOT_NOW).
- 2026-09-05 — Стек: Expo managed + TypeScript + Expo Router; тесты Jest (jest-expo) + MSW v2 (`msw/node` в тестах, `msw/native` в dev-приложении) — потому что Vitest в RN не стандарт, а MSW официально поддерживает RN через `msw/native`.
- 2026-09-05 — Первый срез без карты: маршрут показывается лентой времени; карта — второй срез — потому что ценность в расписании/конфликте, а карта в RN — самая рискованная сборочная часть.
- 2026-09-05 — Работа с Claude Code разделена на две фазы с отдельными документами (`docs/PHASE1_CORE.md`, `docs/PHASE2_APP.md`); интерфейс между ними — `docs/API_CONTRACT.md` — потому что так второй агент стартует с чистым контекстом.
- 2026-09-05 — Скаффолд: `create-expo-app@latest --template default --yes --no-agents-md` во временной папке с переносом в корень — потому что CLI отказывается работать в папке, где лежит `CLAUDE.md`, а без `--no-agents-md` он бы перезаписал наш `CLAUDE.md` своим. Встало: Expo SDK 57.0.20, RN 0.86.3, React 19.2.3, TypeScript 6.0.3.
- 2026-09-05 — Каталог Expo Router — `src/app/` (так устроен шаблон default SDK 57), а не корневой `app/`, как написано в `PHASE2_APP.md` — потому что переносить шаблон ради имени папки нет смысла; фаза 2 кладёт экраны в `src/app/`.
- 2026-09-05 — Jest: `projects` c `jest-expo/node` для `__tests__/core/**` вместо пресета `jest-expo` (iOS) — потому что iOS-окружение задаёт export conditions `['require','react-native']`, под которыми `msw/node` в `msw/package.json` экспортирует `null`, а `expo/src/winter/runtime.native.ts` подменяет `fetch` на `expo/fetch`, который в Jest — заглушка. Фаза 2 добавит второй проект `jest-expo/ios` для экранов.
- 2026-09-05 — `babel.config.js` с `babel-preset-expo` (явно в devDependencies) — потому что платформенные пресеты jest-expo передают в babel-jest только `caller` и ждут конфиг проекта; без него Jest не транспилирует даже наши TS-файлы.
- 2026-09-05 — В Jest транспилируем три ESM-only зависимости msw 2.15 (`rettime`, `@open-draft/deferred-promise`, `until-async`) через `transformIgnorePatterns` — потому что у них нет CJS-сборки, а Jest 29 не умеет `require(esm)`. Список получен перебором всех рантайм-зависимостей `msw/lib/{core,node}` и `@mswjs/interceptors`.
- 2026-09-05 — devDependencies: `@types/jest@^29` (под jest 29.7), `@types/node@^24` (под Node 24); в `tsconfig.json` явный `types: ["jest","node","expo/types"]` — потому что без явного `types` tsc 6.0.3 не находил глобалы jest/node, а `expo/types` даёт декларации CSS-модулей шаблона без сгенерированного `expo-env.d.ts` (он в `.gitignore`, создаётся только `expo start`).
- 2026-09-05 — Мок-сценарий «все парковки заняты» переключается функцией `setMockScenario()` в `src/core/mocks/scenario.ts` (шаг 4); заголовок `X-Mock-Scenario` не вводим — потому что один механизм для тестов и dev-переключателя фазы 2, и `planTrip` о моках не знает. Подтверждено пользователем.
- 2026-09-05 — Имена параметров TomTom Orbis v2 берём из документации, а не из текста `PHASE1_CORE.md` (`pauseTimeInSeconds`, query `vehicleWeight`, координаты в пути URL) — потому что иначе контракт с реальным API не совпадёт. Подтверждено пользователем; точные цитаты фиксируются на шаге 2.
- 2026-09-05 — Схема ответа TomTom взята из https://docs.tomtom.com/routing-api/documentation/tomtom-orbis-maps/v2/calculate-route (прочитано 2026-09-05 из исходного HTML страницы, не из пересказа; выжимка WebFetch дала неверное значение в примере — 10 вместо 1020). Точные имена: путь `/maps/orbis/routing/calculateRoute/{routePlanningLocations}/{contentType}`, локации «Colon-delimited», `location` = «Latitude, longitude pair»; query `key`, `apiVersion=2` («Only allowed value: 2»), `departAt` («must be specified as a dateTime»), `traffic` (`live` | `historical`), `vehicleWeight` («Weight of the vehicle in kilograms»), `travelMode` («Default value and only allowed value: car»), `routeRepresentation=polyline` («returned in the points fields»); тело POST `legs[].routeStop.pauseTimeInSeconds` («It must be 0 in the last leg (the destination)»), «number of elements in the array must be equal to the number of waypoints + 1»; ответ `routes[].summary.{lengthInMeters,travelTimeInSeconds,departureTime,arrivalTime}`, `routes[].legs[].summary`, `routes[].legs[].points[].{latitude,longitude}`. Времени по точкам в `points` нет (есть `progress[]` при `extendedRouteRepresentation`) — остаёмся на пропорции по расстоянию (NOT_NOW).
- 2026-09-05 — Фикстура `tomtom_route_example.json` = пример ответа из документации, из которого удалены только строки-плейсхолдеры `...further points/legs/progresses/sections/routes...` (после удаления поправлены висячие запятые). Значения не менялись; добавлены две запятые, пропущенные в самом примере документации после `"sectionType": "TRAVEL_MODE"` (проверено по исходному HTML — опечатка в документации). Это маршрут 1147 м / 161 с; фикстуры для сквозных тестов (~9 ч) будут синтетическими по этой схеме (шаг 3).
- 2026-09-05 — Координаты внутри ядра и в публичном контракте — `GeoPoint { lat, lon }` (`src/core/types.ts`), на проводе TomTom — `TomTomPoint { latitude, longitude }` (`api/types.ts`) — потому что `PHASE1_CORE.md` везде пишет `lat/lon`, а имена TomTom менять нельзя; конвертация в одном месте, `api/tomtom.ts`.
- 2026-09-05 — Мок TomTom — детерминированный генератор `src/core/mocks/routeGenerator.ts` вместо двух статичных JSON-фикстур из плана — потому что геометрия строится по локациям запроса, число плеч всегда = waypoints + 1, а `departureTime/arrivalTime` считаются от `departAt` с учётом пауз, так что `planTrip` может честно использовать времена из ответа. Подтверждено пользователем. Единственный статичный JSON — пример из документации (шаг 2). Параметры мока (MVP, hardcoded): 100 км/ч, точка каждые 5 км, прямые между локациями.
- 2026-09-05 — `[предположение]` В моке `routes[].summary.travelTimeInSeconds` = только время в движении (сумма плеч без пауз); `arrivalTime` пауз учитывает. Документация описывает travelTimeInSeconds только через трафик, про паузы молчит. Ядро поэтому считает вождение по `legs[].summary.travelTimeInSeconds`, а ETA берёт из `arrivalTime` — эти два поля от предположения не зависят.
- 2026-09-05 — Даты в моке TomTom — ISO в UTC (`…Z`); реальный API отдаёт с оффсетом точки отправления. Сравнивать по `Date.parse`, не по строке; форматирование времени для экрана — забота фазы 2.
- 2026-09-05 — Мок TomTom проверяет документированные ограничения (`key`, `apiVersion=2`, `legs.length = waypoints + 1`, последняя пауза 0) и отвечает 400/403 с нашим форматом `{ error }` — потому что это ловит ошибки `planTrip` в тестах; ядро смотрит только на `res.ok`.
- 2026-09-05 — Закрыто предположение из STATE: babel-preset-expo в не-production не инлайнит `process.env.EXPO_PUBLIC_*`, а заменяет на `env.X` из `expo/virtual/env`, где `env = process.env` — поэтому ключ, заданный в `jest.setup.ts`, доступен в рантайме, и тест «без ключа» может удалять переменную на лету.
- 2026-09-05 — Parking-мок: сценарий `all-full` накладывается в handler поверх результата генератора (`freeAt: 0`), генератор `parkingGenerator.ts` чистый и детерминированный — потому что сценарий — свойство мока, а не данных, и генератор тестируется без сети.
- 2026-09-05 — `confidence` в Parking-моке: «сейчас» = `Date.now()` в handler, генератор получает `now` параметром — потому что иначе тест на `confidence` зависит от часов машины, на которой идут тесты.
- 2026-09-05 — Seed генератора парковок — FNV-1a от строки `${lat},${lon}`, PRNG mulberry32, оба свои (~15 строк), без новых пакетов — потому что PHASE1 запрещает пакеты без нужды, а детерминизм «один вход → один ответ» нужен для тестов и повторного запроса парковок в шаге 7.
- 2026-09-05 — Занятость Parking-мока зависит от координат и периода суток (ночь `[22, 06)` по UTC, иначе день), а не от конкретного часа `at`; час берётся по UTC — потому что повторный запрос по сдвинутому времени в шаге 7 должен быть предсказуем в тестах; локальное время — NOT_NOW.
- 2026-09-05 — Ёмкость парковок 20–80 мест, днём занято 40–70 % (→ `freeAt ≥ 6`), ночью 85–100 % (→ бывает 0) — потому что happy path шага 7 требует свободную парковку на обоих якорях (08:10Z и 13:05Z — второй с учётом паузы на первом; раньше здесь стояло 12:20Z без паузы), а ночью конфликт должен быть возможен и без сценария `all-full`.
- 2026-09-05 — `recordRequests()` вынесен в `__tests__/core/helpers.ts` на шаге 4, раньше запланированного шага 7 — потому что понадобился для проверки query в `parking.test.ts`.
- 2026-09-05 — AETR-пороги включительные: ровно на 4:30 непрерывного и 9:00 суточного вождения ехать дальше нельзя (`>=`); при одновременном достижении приоритет `dailyRest` — потому что «после 4:30 — перерыв» читаем как «отметку 4:30 проезжать нельзя», а суточный отдых поглощает перерыв. Подтверждено пользователем.
- 2026-09-05 — Результат `nextRequiredBreak` — `{ type: 'break45' | 'dailyRest' | null, remainingSec }`, `remainingSec` = до ближайшего из двух порогов, 0 при достижении; константу суточного отдыха 11 ч не вводим — потому что `remainingSec` пригодится планировщику шага 7 (`breaks.ts` в итоге работает напрямую с константами, см. ниже), а 11 ч в MVP никто не читает (планирование дольше дня — NOT_NOW).
- 2026-09-05 — `breaks.ts`: якорь ставится линейной интерполяцией внутри сегмента ровно на пороге 4:10, а не снапом к ближайшей точке полилинии — потому что у мока точки через 5 км (±3 мин), а у реального TomTom точки плотные, и интерполяция даёт одинаково точный результат в обоих случаях.
- 2026-09-05 — `breaks.ts`: очередной якорь не ставится, если от предыдущего якоря (или старта) до финиша меньше `MAX_CONTINUOUS_DRIVING_SEC` — потому что это то же самое, что «до финиша меньше запаса 20 мин», но выражено через норму: водитель легально доезжает без перерыва. Согласуется с включительным порогом: 4:29 → 0 якорей, 4:30 → 1, 8:39 → 1, 8:40 → 2.
- 2026-09-05 — `breaks.ts` не учитывает суточный лимит 9:00, только перерывы 4:30; флаг `requiresDailyRest` ставит `schedule.ts` (шаг 7) — потому что для маршрутов до 13 ч на якоря это не влияет, а планирование дольше дня — NOT_NOW.
- 2026-09-05 — В `Anchor` сверх плана добавлено `drivenSecFromStart` — потому что шагу 7 оно нужно для пересчёта приездов по плечам вызова 2, а тесту — для точной проверки 4:10 / 8:20 без пересчёта по дистанции.
- 2026-09-05 — Якоря ставятся все сразу из вызова 1 (`placeBreakAnchors`), последовательное якорение от выбранной парковки и учёт показаний тахографа не делаем — потому что пользователь решил держаться изначального плана `PHASE1_CORE.md` шага 7; оба варианта записаны в NOT_NOW. Подтверждено пользователем.
- 2026-09-05 — Выбор парковки у якоря — ближайшая по haversine с `freeAt ≥ 1`; после вызова 2 защитная проверка «каждое плечо ≤ 4:30», иначе `conflict` с кодом `leg-exceeds-continuous-limit` — потому что парковки в 25 км от якоря могут сдвинуть остановку до ±15 мин по маршруту и в худшем случае дать плечо 4:40; на тестовом маршруте плечи 4:13. Подтверждено пользователем.
- 2026-09-05 — `conflict.nearestFreeParking` при отсутствии свободных в 25 км берётся одним дополнительным запросом с радиусом `CONFLICT_SEARCH_RADIUS_KM = 50`, только на пути конфликта; при `parking-lost-after-reroute` — из того же повторного ответа, кроме выбранной — потому что иначе полю неоткуда взяться. Подтверждено пользователем.
- 2026-09-05 — `Conflict` содержит машинный `code` и `reason` на английском; код и сообщения — на английском, документы остаются на русском — потому что проект позже переводится на английский, а русские документы нужны владельцу для понимания. Подтверждено пользователем.
- 2026-09-05 — `planTrip` без якорей (маршрут < 4:30) не делает второй вызов TomTom и отвечает по вызову 1 с `stops: []`; `Stop.departAt = arrivalAt + BREAK_SEC` считается сам, а не берётся из `legs[i+1].departureTime`; `drivingTimeSec` и `requiresDailyRest` — по сумме `legs[].summary.travelTimeInSeconds` последнего вызова — потому что второй вызов без waypoints повторил бы первый, а поля плеч не зависят от `[предположение]` про паузы в `summary.travelTimeInSeconds`.
- 2026-09-05 — Проверка «всё сходится»: `|приезд по вызову 2 − plannedArrivalAt| > MAX_ARRIVAL_SHIFT_SEC = 1800` → один повторный `getParkingsNear` по координатам якоря на фактическое время; ок, если выбранная парковка всё ещё `freeAt ≥ 1`; иначе `parking-lost-after-reroute`, без третьего вызова TomTom — потому что более одной итерации — NOT_NOW, а запрос по координатам якоря даёт тот же seed, и выбранная парковка гарантированно в ответе.
- 2026-09-05 — `src/core/index.ts` не экспортирует `mocks/native` вопреки тексту `PHASE1_CORE.md` шага 8; приложение подключает моки по прямому пути `src/core/mocks/native.ts` под `__DEV__` — потому что иначе msw и полифиллы попадают в прод-бандл; проверяется тестом `index.test.ts`.
- 2026-09-05 — Рецепт включения моков в приложении (`API_CONTRACT.md` § 6) написан через `require` внутри `if (__DEV__)` и `onUnhandledRequest: 'bypass'` — потому что ES `import` поднимается и ломает порядок «полифиллы → msw/native», а `'error'` уронил бы запросы Metro/Expo; значения опции проверены по типам msw 2.15.0, сам рецепт в приложении не запускался (`[предположение]`, первая проверка фазы 2).
- 2026-09-05 — Parking-мок переведён на сетку: мир разбит на ячейки 0.3°, в каждой детерминированно 1–2 парковки с фиксированными id, позицией, ёмкостью и коэффициентом занятости (seed от индексов ячейки); запрос любого центра и радиуса — подмножество одного множества, ближайшие первыми — потому что аудит показал: при seed от центра запросы на 25 и 50 км давали одинаковые id и `freeAt` с разными координатами, и запасной запрос на 50 км для `conflict.nearestFreeParking` не мог найти свободную парковку. «3–6 парковок в радиусе» из `PHASE1_CORE.md` теперь не константа, а плотность: у тестового якоря 3 в 25 км, 19 в 50 км. Сигнатура `generateParkings`, handler и `planTrip` не менялись; в `parking.test.ts` +3 теста на стабильность (50 ⊇ 25, соседний центр, порядок по расстоянию), проверка «3–6» заменена на «≥ 1».
- 2026-09-05 — Фаза 2 стартовала: в `docs/PHASE2_APP.md` пути экранов исправлены на `src/app/*` (три строки, остальной текст не менялся); корневой layout — `Stack` с экранами `index` → `planning` → `result`, табы шаблона (`explore.tsx`, `app-tabs*.tsx`) удаляются; в `planTrip` добавляется необязательный колбэк `onProgress(stage)` вторым аргументом — единственная правка `src/core` в фазе 2 — потому что сценарий водителя линейный, а стадии экрана 2 по таймеру были бы имитацией. Подтверждено пользователем.
- 2026-09-05 — Моки в приложении подключены по рецепту `API_CONTRACT.md` § 6 в `src/app/_layout.tsx`, плюс лог `[msw] <method> <url>` через `server.events.on('request:start')` под `__DEV__` — потому что критерий шага 2 «в консоли Metro видно перехваченный запрос» иначе нечем показать. Проверено на web (Chrome): `fetch` на `mock.convoy-mind.local` перехвачен, 200, 3 парковки. На web Metro использует condition `browser`, под которым `msw/native` экспортирует `null`, и откатывается на file-based резолв (WARN «invalid package.json configuration … Falling back» от `msw/native` и `@mswjs/interceptors/*` — ожидаемы, не чинить). На iOS/Android condition `react-native` матчится напрямую `[предположение]` — проверка в Expo Go у пользователя.
- 2026-09-05 — Тесты экранов: второй Jest-проект `screens` на пресете `jest-expo/ios` (`__tests__/screens/**/*.test.tsx`), `planTrip` ходит через тот же MSW, что и ядро, без `jest.mock` — потому что так требует критерий `PHASE2_APP.md`. Две точечные обходки RN-окружения вместо смены пресета: (1) `moduleNameMapper` `^msw/node$` → `msw/lib/node/index.js`, так как под conditions `['require','react-native']` (`@react-native/jest-preset/jest/react-native-env.js`) `msw/node` экспортирует `null`; (2) в `jest.setup.screens.ts` `globalThis.fetch = globalThis.originalfetch` — `expo/src/winter` подменяет `fetch` на заглушку `expo/fetch`, а `installGlobal` в `__DEV__` сохраняет оригинал под именем `original` + имя без капитализации (приоритет `??` в `installGlobal.ts`). Проверено пробным тестом: `Headers/Request/Response` остаются нативными Node, перехват MSW работает.
- 2026-09-05 — `@testing-library/react-native@13.3.3` + явный `react-test-renderer@19.2.3` в devDependencies — потому что `expo-router` 57 dev-зависит от RNTL `^13.3.0` (peer `>= 13.2.0`), RNTL 14 требует новый peer `test-renderer`, а без явного пина npm тянул `react-test-renderer@19.2.8` с peer `react@^19.2.8` и падал в ERESOLVE (в проекте `react@19.2.3`, версия из `jest-expo`).
- 2026-09-05 — Состояние поездки между экранами — модульный стор `src/ui/tripStore.ts` (`startPlanning`, `useTripState` на `useSyncExternalStore`, `resetTrip`, счётчик запуска против поздних ответов); `planTrip` вызывается с экрана 1, экран 2 ждёт `done` и делает `router.replace('/result')` — потому что `PlanResult` слишком велик для query-параметров, а контекст/библиотека стейта для трёх экранов избыточны.
- 2026-09-05 — Пресеты экрана 1: Варшава, Лодзь, Франкфурт, Париж (`src/ui/presets.ts`) — Париж вне «Польши/Германии» из `PHASE2_APP.md`, но это единственный пресет контракта § 5 с `requiresDailyRest: true`. Время выезда уходит как RFC 3339 с оффсетом устройства (`toRfc3339Local`), как просит контракт § 4.
- 2026-09-05 — Ключ TomTom в приложении: локальный `.env` с `EXPO_PUBLIC_TOMTOM_KEY=mock-dev-key` (в `.gitignore`) — потому что без ключа `planTrip` бросает до сети, а мок проверяет только непустоту.
- 2026-09-05 — `planTrip(input, options?)` с `options.onProgress(stage)`, стадии `route → anchors → parkings → schedule` (`PlanStage`, `PlanOptions` экспортированы из `@/core`); при маршруте без остановок все четыре стадии всё равно сообщаются, при conflict/ошибке — обрываются — потому что экран 2 должен показывать реальный прогресс, а не таймер (`PHASE2_APP.md`, экран 2). Единственная правка `src/core` в фазе 2; тесты фазы 1 не тронуты, новый `__tests__/core/progress.test.ts`. Подтверждено пользователем.
- 2026-09-05 — Экран 2 держится минимум `PLANNING_MIN_VISIBLE_MS = 1200` мс (`src/ui/planningStages.ts`) до перехода на результат — потому что с моками `planTrip` отрабатывает за миллисекунды и четыре стадии иначе не видны. Это пауза показа, а не имитация: стадии зажигаются по `onProgress` из ядра. Тесты экранов ждут переход с `timeout: PLANNING_MIN_VISIBLE_MS + 2000`.
- 2026-09-05 — Экран 3, лента: последнее плечо до финиша считается на экране как `drivingTimeSec − Σ stops[].drivingSecFromPrev` — потому что отдельного поля в `PlanResult` нет, а `drivingTimeSec` по контракту § 4 — ровно сумма плеч, так что это арифметика, а не новая логика. Если владелец предпочтёт поле в ядре (`finalLegDrivingSec` или `legs[]`) — одна строка в `okResult`, вопрос записан в STATE.
- 2026-09-05 — Экран 3, индикатор `confidence`: `>= 0.9` → «сейчас», иначе «прогноз» — потому что контракт § 2 определяет ровно два значения (0.9 «в пределах часа от сейчас», 0.6 прогноз).
- 2026-09-05 — «Выехать раньше» уводит на экран 1 с параметром маршрута `departureAt` (RFC 3339, `input.departureAt − 30 мин`); экран 1 показывает его выбранным чипом «в HH:MM», любой чип сдвига его сбрасывает — потому что чипы сдвигов считаются от «сейчас», а «раньше на 30 мин» должно считаться от прошлого выезда. Константа `EARLIER_SHIFT_MIN = 30` в `src/app/result.tsx`.
- 2026-09-05 — «Другая парковка» показывает «Скоро» надписью на самой кнопке, без `Alert`/тоста — потому что кросс-платформенного тоста в RN нет, `Alert` на web не работает, а функция в NOT_NOW.
- 2026-09-05 — В `tripStore` добавлен `setTripState(next)` (сбрасывает текущий запуск) — потому что тесты экрана 3 рендерят фикстуры `PlanResult` из контракта § 5 без сети (`PHASE2_APP.md`, шаг 5); фикстуры извлекаются из `docs/API_CONTRACT.md` скриптом в `__tests__/screens/fixtures.ts`, не переписываются руками.
- 2026-09-05 — Экран 1 читает начальное положение dev-переключателя из `getMockScenario()` — потому что сценарий живёт в модуле ядра и переживает переходы, а `Switch` с локальным `false` показывал бы неверное состояние после «Новый маршрут».
- 2026-09-06 — Фаза 3 стартовала. Подтверждено пользователем: (1) ключ TomTom в локальном `.env` настоящий, но `EXPO_PUBLIC_TOMTOM_LIVE=0` по умолчанию — живой вызов проверяется вручную на шаге 6, значение ключа не попадает ни в один трекаемый файл; (2) `route.points` в § 5 контракта сокращён, скрипт сверяет всё остальное; (3) карта заменяет ленту на экране 3, 6 тестов ленты в `__tests__/screens/result.test.tsx` будут переписаны на шаге 8 — это следствие решения «карта вместо ленты», а не ослабление тестов.
- 2026-09-06 — Расхождения `PHASE3_MAP.md` с текущим кодом (документ писался в расчёте, что 3a пойдёт до фазы 2): второй Jest-проект заводить не нужно — `screens` (`jest-expo/ios`) уже есть, `jest.config.js` не трогаем; тесты экрана 3 идут в `__tests__/screens/`, а не в `__tests__/app/`.
- 2026-09-06 — Шаг 1: `PlanResult.route = { points: GeoPoint[] }` собирается в `okResult()` из того же `routePoints(route)`, что уже использовался для якорей — потому что `okResult` получает `route2` на happy path и `route1` на маршруте без якорей, так что «геометрия последнего вызова TomTom» получается без отдельной ветки.
- 2026-09-06 — Сверка § 5 контракта — не отдельный скрипт, а тест `__tests__/core/contract.test.ts` в проекте `core` — потому что ядро использует безрасширенные импорты (`'../api/parking'`), которые Node ESM не резолвит, а ставить загрузчик TS ради одного скрипта запрещено правилом «не придумывать пакеты». Тест сверяет постоянно, а не однократно. Режим `UPDATE_CONTRACT=1 npx jest --selectProjects core contract` перезаписывает § 5 и `__tests__/screens/fixtures.ts` из реального вывода — потому что фикстуры экранов раньше генерировались одноразовым скриптом, которого в репозитории нет, и руками их править нельзя.
- 2026-09-06 — В § 5 `route.points` показан первыми 3 и последней точками, полное число объявлено маркером `<!-- contract:ok points=181 head=3 tail=1 -->` — потому что критерий 3a.4 требует сверки с реальным выводом, а полные 181 точка сделали бы контракт нечитаемым. Тест сверяет число точек и совпадение показанных с началом и концом реального массива; фикстуры экранов получают тот же сокращённый массив.
- 2026-09-06 — Шаг 2, обход коридора: запросы на прогрессе `0, stepKm, 2·stepKm, …` пока прогресс `< totalM` — потому что это ровно `ceil(totalM / stepKm)` запросов, как требует критерий 3a.2 (для тестового маршрута 36), и не зависит от того, кратна длина шагу или нет. Запросы последовательные, не `Promise.all` — порядок в `recordRequests` детерминирован, а мок в процессе, так что 36 запросов идут за миллисекунды.
- 2026-09-06 — Шаг 2, `routeProgressM` парковки — прогресс ближайшей вершины полилинии (проекция на вершину, не на отрезок) — потому что у мока вершины через 5 км, у реального TomTom плотнее, и для порядка маркеров на карте этого достаточно; проекция на отрезок — лишняя точность в MVP.
- 2026-09-06 — Шаг 2, ETA в точке = `departureAt + (прогресс/всего)·drivingTimeSec + 45 мин × число остановок строго ДО этой точки`. Строгое «до» — потому что у самой выбранной парковки `etaAt` должен быть приездом, а не отъездом после перерыва (первая версия считала `<=` и давала 08:56 вместо 08:11 — ошибка найдена на реальном выводе, закрыта тестом «etaAt выбранной парковки — приезд, а не отъезд»).
- 2026-09-06 — Шаг 2, при попадании парковки в несколько запросов берётся первый по ходу маршрута — потому что дедуп нужен по `id`, а занятость в моке зависит только от периода суток; `at` этого запроса может отличаться от `etaAt` самой парковки не более чем на `corridorKm` / скорость (~9 мин на 100 км/ч). Помечено в JSDoc `corridor.ts`.
- 2026-09-06 — Шаг 2, `getParkingsAlongRoute` принимает `stops: Stop[]` из `schedule.ts` — потому что так написано в `PHASE3_MAP.md` и экран передаёт `result.stops` как есть; цикла импортов нет (`schedule.ts` про `corridor.ts` не знает).
- 2026-09-06 — Шаг 3, `MOCK_LIVE_AMPLITUDE = 0.35` (доля ёмкости) — потому что днём занято 40–70 %, и дрейфа в 10–15 % не хватало бы, чтобы парковка дошла до нуля: `freeAt` менялся бы, а цвет маркера на карте — нет, и критерий 3b («за минуту меняется цвет хотя бы одного маркера») не выполнялся бы. Значение подобрано не на глаз: замер по 45 парковкам коридора Варшава → Франкфурт дал 3–9 смен статуса и ~40 смен `freeAt` за минуту.
- 2026-09-06 — Шаг 3, дрейф живёт в генераторе (флаг `live` в `GenerateParkingsInput`), а не накладывается в handler поверх результата, как `all-full` — потому что `all-full` — это подмена значения (`freeAt: 0`), а дрейф зависит от `capacityTotal` и seed конкретной парковки, то есть от внутренностей генератора. Генератор остаётся чистым: сценарий приходит параметром, handler его только читает.
- 2026-09-06 — Шаг 3, seed дрейфа — `FNV-1a("<id>@<номер минутного слота>")`, слот = `floor(now / 60_000)` — потому что нужен один и тот же ответ в пределах минуты (иначе экран мигал бы на каждом запросе) и разный между минутами; id парковки в seed — чтобы стоянки дрейфовали независимо, а не хором.
- 2026-09-06 — Шаг 4, реальный TomTom включается только точным `EXPO_PUBLIC_TOMTOM_LIVE=1`; выбор набора handlers вынесен в чистую функцию `appHandlers({ tomtomLive })` в `handlers.ts`, а `native.ts` её только вызывает — потому что `native.ts` импортирует `msw/native`, который в Jest-проекте `core` не грузится, и без этого шаг 4 остался бы непроверяемым «должно работать». Проверено тестом `__tests__/core/tomtom-live.test.ts`, включая негативный прогон: если убрать фильтр, падают 2 теста.
- 2026-09-06 — Шаг 4, Parking-API мокается **всегда**, даже при `LIVE=1` — потому что хост `mock.convoy-mind.local` не существует; opt-in касается только TomTom.
- 2026-09-06 — Шаг 4, `jest.setup.ts` и `src/core/mocks/server.ts` не тронуты: тесты всегда на моке, `EXPO_PUBLIC_TOMTOM_LIVE` в них не выставляется. В `tomtom-live.test.ts` поднимается отдельный сервер с набором LIVE, и на это время глобальный сервер закрывается — потому что иначе слушают двое и порядок перехвата зависит от реализации msw, а не от нашего кода.
- 2026-09-06 — `[предположение]` Тарифы TomTom: на https://docs.tomtom.com/pricing (прочитано 2026-09-06) заявлено «Free monthly requests per API, no credit card needed», для Routing API — «Free 20K monthly», тег «TomTom Orbis Maps» на этом же продукте. Число 20K получено выжимкой WebFetch и **вторым источником не подтверждено** (поиск его не вернул, страница JS-heavy); прошлая сессия уже ловила WebFetch на неверном числе в примере документации. Качественная часть (бесплатный тариф есть, карта не нужна) повторяется на странице несколько раз. Для нас риск мал: `planTrip` делает 1–2 вызова TomTom на расчёт, а `LIVE=0` по умолчанию. Проверить точное число — в личном кабинете разработчика перед включением `LIVE=1`.
- 2026-09-06 — Шаг 6, `react-native-maps@1.27.2` (поставлен `npx expo install`, версия под SDK 57). API прочитан по `node_modules/react-native-maps`, не по памяти: `MapView` (default, `initialRegion?: Region`, `onMapReady?`, ref-метод `fitToCoordinates(coords, { edgePadding, animated })`), `Marker` (`coordinate: LatLng`, `pinColor?`, `title?`, `description?`), `Polyline` (`coordinates: LatLng[]`, `strokeColor?`, `strokeWidth?`), `LatLng = { latitude, longitude }` (конвертация из нашего `GeoPoint { lat, lon }`).
- 2026-09-06 — Шаг 6, callout — штатные `title`/`description` вместо дочернего `<Callout>` — потому что по исходнику `MapMarker.tsx` (строки 111, 286–288) `title`/`description` работают ровно тогда, когда дочернего `<Callout />` нет, а кастомная вью внутри `Marker` заменяет сам пин. Штатный вариант проще и без риска.
- 2026-09-06 — **Шаг 6, `react-native-maps` не работает под `react-native-web` и роняет ВЕСЬ бандл**, а не только карту: `src/specs/NativeComponent{Marker,Polyline,…}.ts` импортируют `codegenNativeComponent` из `'react-native'`, которого в `react-native-web` нет (`(0 , _reactNativeWebDistIndex.codegenNativeComponent) is not a function`), а Expo Router валидирует экспорты всех маршрутов при старте — на web ложился и экран 1. У `MapView` web-вариант есть (`MapView.web.ts` → `UnimplementedView`), у `Marker`/`Polyline` — нет. Решение (подтверждено пользователем): карта вынесена в пару `src/ui/routeMap.tsx` (натив) / `src/ui/routeMap.web.tsx` (список тех же данных без пакета), Metro подставляет `.web` сам; `src/app/result.tsx` остаётся один и пакет не импортирует. Причина выбора: web — единственный канал визуальной проверки у агента и рабочая платформа с фазы 2, а карта на web всё равно была бы пустым прямоугольником.
- 2026-09-06 — Шаг 6, `npx expo export --platform web` **проходит**, а `npx expo start --web` на том же коде падает — на эту разницу я сначала купился и сделал неверный вывод «web не ломается». Вывод для следующих сессий: проверять web только живым dev-сервером (`curl` на `/` и `/result`, в html не должно быть `_expo-static-error`), успех `export` ничего не доказывает.
- 2026-09-06 — Шаг 6, мок `react-native-maps` для Jest — один файл `__tests__/screens/maps-mock.tsx`, подключается в `jest.setup.screens.ts`, а не `jest.mock(...)` в каждом тесте — потому что экран 3 монтируется в четырёх файлах тестов. Мок зовёт `onMapReady` **один раз** (как настоящий `MapView`), иначе `fitToCoordinates` перезапускался бы на каждом ре-рендере.
- 2026-09-06 — Шаг 6, `freeLine` и `parkingCallout` переехали в `src/ui/text.ts` — потому что нужны и нативной карте, и web-списку, и карточке конфликта; дублировать в трёх местах хуже, чем один общий текстовый модуль, который уже существовал.
- 2026-09-06 — Шаг 7, обновление парковок — `setInterval(load, MAP_REFRESH_MS = 30_000)` внутри того же `useEffect`, что делает первый запрос, с общим `alive`-флагом и `clearInterval` в cleanup — потому что источник данных один и отменяться должны оба пути. Живёт в `src/app/result.tsx` (платформонезависимая часть), карта на native/web не знает про таймер.
- 2026-09-06 — Шаг 7, dev-переключатели «все парковки заняты» и «live» взаимоисключающие (одно состояние `scenario` вместо двух булевых) — потому что в моке сценарий один (`MockScenario | null`), и два независимых `Switch` показывали бы невозможное состояние.
- 2026-09-06 — Шаг 7, «Перепланировать» вызывает `startPlanning(input, labels)` и уводит на `/planning` — потому что это уже существующий путь со стадиями, а не второй способ считать маршрут. Считает **от старта с тем же входом**, не от текущей позиции водителя (это в `NOT_NOW.md`).
- 2026-09-06 — Шаг 7, подпись «обновлено HH:MM:SS» — новый `formatClockSec(ms)` в `src/ui/time.ts` рядом с `formatClock` — потому что существующий `formatClock` принимает ISO-строку и режет секунды, а тут нужен `Date.now()` с секундами.
- 2026-09-06 — Шаг 8, новый Jest-проект для `__tests__/app/**` не заводился вопреки тексту `PHASE3_MAP.md`: проект `screens` (`jest-expo/ios`) существует с фазы 2, тесты экрана 3 лежат в `__tests__/screens/`, `jest.config.js` не тронут — потому что документ писался в расчёте, что часть 3a пойдёт до фазы 2.
- 2026-09-06 — Шаг 8, тест смены цвета по сценарию `'live'` использует `jest.useFakeTimers({ now })`: модерн-таймеры Jest двигают и `Date.now`, от которого зависит минутный слот дрейфа. Старт на границе минуты, два тика по 30 с: первый обязан дать тот же ответ (та же минута), второй — другой. Так проверяются оба свойства сразу.

- 2026-09-06 — **Падение в Expo Go `Property 'MessageEvent' doesn't exist`.** Причина `[проверено]`: `msw` 2.15 требует DOM-классы уже при загрузке модулей — зависимость `rettime` объявляет `class TypedEvent extends MessageEvent`, а `msw/lib/core/ws` на верхнем уровне делает `new BroadcastChannel(...)` (его тянет любой `import { http } from 'msw'`). RN 0.86 глобально ставит только `Event`, `EventTarget`, `CustomEvent` (`react-native/src/private/setup/setUpDOM.js`); `MessageEvent` в исходниках RN есть (`src/private/webapis/html/events/MessageEvent.js`), но наружу не выставлен. Известная проблема msw в RN: https://github.com/mswjs/mswjs.io/issues/453; официальные полифиллы MSW (`URL`, `TextEncoder`) её не закрывают.
- 2026-09-06 — Фикс: `src/core/mocks/domPolyfills.ts` с чистой `installMswDomPolyfills(globalThis)` (ставит `MessageEvent extends Event` с полями `data`/`origin`/`lastEventId` как в реализации RN и заглушку `BroadcastChannel extends EventTarget` с пустыми `postMessage`/`close`), вызов — в `msw.polyfills.js` после двух существующих импортов. Отдельный модуль в `src/core/mocks/`, а не код внутри `msw.polyfills.js`, — потому что его надо покрыть тестом, а `msw.polyfills.js` в Jest не грузится. Импортов `react-native` нет, правило «ядро без React» не нарушено. Идемпотентна: на web оба класса есть и функция ничего не делает; при отсутствии базовых `Event`/`EventTarget` бросает понятную ошибку вместо `undefined is not a constructor`.
- 2026-09-06 — `BroadcastChannel` — именно заглушка, а не рабочий канал: msw использует его для синхронизации WebSocket-моков между вкладками браузера, а мы WebSocket не мокаем. Реализовывать межконтекстную доставку в RN не за что.
- 2026-09-06 — Проверка фикса — Node с удалёнными `MessageEvent`/`BroadcastChannel` и условием резолва `--conditions=react-native` (то же условие, что берёт Metro на iOS/Android): без полифиллов `require('msw/native')` даёт `ReferenceError: MessageEvent is not defined`, с ними — `installed: MessageEvent, BroadcastChannel`, сервер поднимается и перехватывает `GET https://example.test/native`. Это **не эмуляция Hermes**: движок остаётся V8 с глобалами Node минус два удалённых, так что проверен резолв и достаточность двух классов, а не весь рантайм телефона. Плюс Jest-тест `__tests__/core/domPolyfills.test.ts` — потому что симулятора iOS/Android на машине нет, а «поставил и надеюсь» — это то самое непроверенное место, из-за которого баг и дожил до телефона. `[предположение]` снято с **причины**, но не с самого Expo Go: живой запуск после фикса делает владелец.
- 2026-09-06 — Полифиллы остаются в `_layout.tsx` (не в entry-шиме перед `expo-router/entry`) — потому что проверено grep: ни один модуль маршрута не тянет `msw` на верхнем уровне (`@/core/index.ts` моки не экспортирует, `scenario.ts` без импортов, `mocks/{handlers,server,native}.ts` вне `mocks/` импортирует только `_layout.tsx`). Expo Router вычисляет модули всех маршрутов при старте, так что если появится маршрут с `import ... from 'msw'` наверху, падение вернётся — тогда нужен шим. Записано в `API_CONTRACT.md` § 6.
- 2026-09-06 — В тесте полифилла тип конструктора описан локально (`MessageEventPolyfillCtor`), а не через `typeof MessageEvent` — потому что у `lib.dom` `MessageEvent<T>` конструктор дженерик, и `class Typed extends (g.MessageEvent as typeof MessageEvent)` даёт `TS2508`. Ошибка была в первой версии теста, найдена `npx tsc --noEmit` (осторожно: `tsc | tail` показывает exit-код `tail`, а не `tsc`).
- 2026-09-06 — **Вторая ошибка на телефоне: `JSON Parse error: Unexpected end of input` после `[msw] POST …/calculateRoute`.** Фикс `MessageEvent`/`BroadcastChannel` сработал (msw/native загрузился и перехватил запрос), но мок-ответ пришёл с HTTP 200 и **пустым телом**. Причина `[проверено]` по исходникам и воспроизведением: глобальный `Response` в RN 0.86 — из `whatwg-fetch` (`react-native/Libraries/Network/fetch.js` → `require('whatwg-fetch')`), а он построен на XMLHttpRequest и стримов не знает вообще — слова `ReadableStream` в его исходнике нет, `Response.prototype.body` не существует. Expo SDK 57 подменяет только глобальный `fetch` (`expo/src/winter/runtime.native.ts`, `install('fetch', …)`; выключается `EXPO_PUBLIC_USE_RN_FETCH=1`), а `Headers`/`Request`/`Response` оставляет от RN. `@mswjs/interceptors` 0.41.9 собирает мок-ответ как `new FetchResponse(decompressResponse(raw) || raw.body, {…})` — одинаково в node- и browser-сборке; `content-encoding` у мока нет, `createDecompressionStream('')` возвращает `null`, значит телом становится `raw.body` = `undefined` → пустое тело.
- 2026-09-06 — Фикс: `installMswResponseBodyShim(globalThis)` в том же `src/core/mocks/domPolyfills.ts`, вызов — в `msw.polyfills.js`. Геттер `Response.prototype.body` отдаёт `_bodyInit` (строку/Blob/`null`), а не `ReadableStream`. Отступление от спецификации сознательное: настоящий `ReadableStream` конструктор `whatwg-fetch` не понимает и кладёт в тело строку `"[object ReadableStream]"` — проверено, вариант хуже исходного бага. Отдельная функция, а не часть `installMswDomPolyfills`, — потому что это не «недостающий DOM-класс», а шим над неспецификационным `Response`, и условие его безопасности другое. Ставится только если `'body' in Response.prototype === false`: на web и в Node — no-op, и no-op же, если будущий Expo SDK привезёт стримовый `Response` (проверки `Platform.OS` нет, иначе `react-native` попал бы в `src/core`).
- 2026-09-06 — Граница безопасности шима записана в JSDoc: `msw` читает `response.body.getReader()` в `core/utils/internal/observe-response-body-stream.js`, но зовёт его из `RequestHandler.complete` только когда резолвер обратился к `finalize` (стриминг, SSE, отложенная очистка). Наши handlers к `finalize` не обращаются — проверено по `RequestHandler.js:155–180, 318–330`. Появится такой handler — шима станет мало, и придётся делать полноценный стримовый `Response`.
- 2026-09-06 — Тест шима — на стенд-ине (`class WhatwgResponseStandIn { _bodyInit }`), а не на настоящем `whatwg-fetch`: под Jest глобальный `Response` — из Node, у него тело-стрим есть, и баг не воспроизводится, а импортировать `whatwg-fetch` в тест нельзя — его нет в `package.json` (он транзитивный от `react-native`). Сквозная проверка на настоящем классе вынесена в диагностический `scripts/msw-native-repro.cjs`: `node scripts/msw-native-repro.cjs` компилирует сам модуль полифиллов, подменяет глобалы на whatwg-fetch, резолвит msw с условием `react-native` и прогоняет два варианта — `bare` (обязан воспроизвести `Unexpected end of JSON input`) и `fixed` (calculateRoute, parkings, 403 с телом, 204 без тела). Exit 0 — фикс на месте.
- 2026-09-06 — **Expo Go на телефоне подтверждён владельцем: «так всё работает».** Это снимает главный `[предположение]` фаз 2 и 3 — единственную непроверенную платформу с фазы 2. Формулировка в документах намеренно осторожная: подтверждение общее, поэлементно (полилиния, маркеры и их цвета, `fitToCoordinates`, callout, таймер 30 с, dev-переключатели) владелец ничего не перечислял, и выдавать это за поштучную проверку нельзя. Код в этот заход не менялся.
- 2026-09-06 — Разбор обоих багов msw в React Native вынесен в `STATE.md`, раздел «Что случилось на телефоне 2026-09-06» — в блок «Для следующего агента», а не в конец файла: следующая сессия начинает чтение оттуда, а стык «msw ↔ RN» — единственное место проекта, где зелёные тесты и рабочий web ничего не гарантируют.
- 2026-09-06 — `[безопасность]` Ключ TomTom засветился в переписке 2026-09-06 в открытом виде — внутри URL запроса из лога Metro (`?key=…`). В трекаемые файлы он не попадал (`.env` в `.gitignore`), но приватным больше не считается. Перевыпуск — в кабинете TomTom, пункт 1 в «Следующий шаг» `STATE.md`. В документы и код значение не пишем ни при каких условиях.
- 2026-09-06 — `[безопасность]` Аудит на утечки перед пушем. Проверено, а не предположено: ключ `EXPO_PUBLIC_TOMTOM_KEY` **никогда не был в git** — просканированы все blob-объекты репозитория, включая недостижимые (`git cat-file --batch-all-objects` + grep по содержимому), 0 совпадений; `.env` не отслеживается и не отслеживался. Репозиторий приватный: `git ls-remote` отвечает, а GitHub API без авторизации даёт 404 на `repos/vla2oc/convoymind`. Широкий скан всего дерева (включая gitignored) на приватные ключи, `AKIA…`, JWT, `ghp_…`, `sk-…` и пары `token/secret/password = <значение>` — пусто. Персистентности пользовательских данных нет (`AsyncStorage`/`SecureStore`/`localStorage` не используются), внешний адрес ровно один — `api.tomtom.com`.
- 2026-09-06 — `[безопасность]` **`grep` в сессии агента может оказаться обёрткой над ugrep с `--ignore-files`, которая молча пропускает всё из `.gitignore`.** Первый скан на ключ дал «чисто», хотя ключ лежал в `.env` и в `.expo/dev/logs/*.log`. Для поиска секретов звать `command grep`, иначе результат ложноотрицательный. Записано, потому что на эти грабли наступит любая следующая сессия.
- 2026-09-06 — `[безопасность]` Ключ **вкомпилирован в production-бандл** — это свойство префикса `EXPO_PUBLIC_`, а не ошибка. Проверено сборкой `npx expo export --platform web` во временную папку вне проекта: в минифицированном `entry-*.js` `process.env.EXPO_PUBLIC_TOMTOM_KEY` заменён на строковый литерал (`calculateRoute=async function(t){const s=await fetch(o(t,"<ключ>")…`). Оттуда же хорошая новость: `msw` и лог `[msw]` в production-бандл не попадают — `if (__DEV__)` действительно вырезается. Вывод: клиентский ключ защищается только ограничениями на стороне TomTom (домен/bundle-id) или прокси-бэкендом; `.gitignore` тут ни при чём. Временная папка с ключом удалена.
- 2026-09-06 — `[безопасность]` Лог MSW в `src/app/_layout.tsx` печатает URL с заменой `key=***` вместо полного `request.url` — потому что настоящий ключ уходил в Metro-вывод, в `.expo/dev/logs/*.log` (16+ вхождений) и, главное, в любую вставку этих строк в `DECISIONS.md`/`STATE.md`, которые git отслеживает. Остальные query-параметры оставлены: по `lat`/`lon`/`at` идёт отладка парковок.
- 2026-09-06 — `[безопасность]` `.gitignore`: `.env*.local` и отдельная строка `.env` заменены на `.env*` с исключением `!.env.example` — потому что Expo читает ещё `.env.development`, `.env.production`, `.env.staging`, и они ушли бы в коммит вместе с ключом. Проверено `git check-ignore` по семи именам; `.env.example` остаётся видимым и в индексе.
- 2026-09-06 — `[безопасность]` Логи `.expo/dev/logs/{start,export}.log` удалены — ключ лежал в них открытым текстом. `~/.expo` и история shell проверены: чисто.
- 2026-09-06 — `[безопасность]` **Остаточное, нашей правкой не закрывается: Expo CLI сам пишет в `.expo/dev/logs/start.log` запись `{"_e":"env:load", …, "env":{"EXPO_PUBLIC_TOMTOM_KEY":"<значение>"}}` при каждом старте.** Обнаружено `[проверено]` тем, что удалённый лог через две минуты вернулся: в новом файле 115 строк `[msw]` и **ни в одной ключа нет** (`key=***` — фикс работает на живом приложении), но одно вхождение осталось — как раз `env:load` от CLI, вне нашего кода. Значит `.expo/dev/logs/` надо считать файлами с секретом: они в `.gitignore` (в git не попадут), но не прикладывать к баг-репортам и не расшаривать. Удалять бессмысленно — возвращается на следующем `expo start`. Ещё один довод за ограничения ключа на стороне TomTom: значение и так живёт в бандле и в логах CLI.
- 2026-09-06 — `[безопасность]` pre-commit хук: исходник в `scripts/hooks/pre-commit` (отслеживается), в `.git/hooks/pre-commit` — симлинк на него. Блокирует env-файлы в индексе (кроме `.env.example`) и содержимое, похожее на секрет. Первая версия ловила саму документацию (`EXPO_PUBLIC_TOMTOM_KEY=...` в пяти местах `docs/`) — паттерн ужесточён до значения из 16+ символов ключевого алфавита; проверено: 0 ложных срабатываний по всем отслеживаемым файлам. Сквозной тест в отдельном репозитории: обычный файл — успех, `.env` — блок, `key=<32 символа>` в `.md` — блок, `.env.example` плюс проза про ключ — успех. Ограничение записано в самом хуке: обходится через `--no-verify` и не переезжает при клонировании — это подстраховка от невнимательности, а не защита.
- 2026-09-06 — `[безопасность]` `npm audit` — 14 moderate, все транзитивные (`decode-uri-component` DoS через `expo-router`, `uuid` bounds через `expo-splash-screen`). Не чиним: доступен только `npm audit fix --force` с ломающими даунгрейдами `expo-router@5.1.11` и `expo-splash-screen@55`, риск от них выше риска самих дыр.
- 2026-09-06 — `[безопасность]` `docs/source/*.docx|pdf` (бизнес-план, VPC, портрет первого клиента) остаются в репозитории — персональных данных внутри нет, репозиторий приватный. Решение пересмотреть, если репозиторий станет публичным.

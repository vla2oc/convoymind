# NOT_NOW

Одна строка на пункт. Не спорим второй раз.

- Габариты грузовика (высота/ширина/длина, ADR): Orbis v2 не поддерживает; вернуться к TomTom v1 или HERE, если станет критично.
- Отдельный бекенд-сервер, скрывающий ключ TomTom.
- Геокодинг адресов (сейчас пресеты с координатами).
- Реальные данные парковок (Autobahn GmbH API для Германии) вместо мока.
- AETR: разбиение перерыва 15+30, продление до 10 ч дважды в неделю, недельные/двухнедельные лимиты, экипаж из двух водителей, сокращённый суточный отдых 9 ч.
- Планирование маршрута дольше одного рабочего дня (сейчас: при >9 ч вождения только флаг requiresDailyRest).
- Более одной итерации «парковка занята → другая парковка» в planTrip.
- Точное время по точкам маршрута (сейчас: пропорционально расстоянию).
- Кнопка «Другая парковка» на экране конфликта (сейчас: показывает «Скоро»).
- Экран 3: отдельное поле последнего плеча в `PlanResult` (сейчас: `drivingTimeSec − Σ drivingSecFromPrev` на экране).
- Экран 1: произвольное время выезда (сейчас: чипы «Сейчас/+30 мин/+1 ч/+2 ч» и фиксированное время только из «Выехать раньше»).
- Прод-сборка без моков: реальный Parking-API и ключ TomTom (сейчас: `msw/native` под `__DEV__`, `.env` с любым непустым ключом).
- Мониторинг в пути, алерты, пересчёт ETA, отчёт план/факт, диспетчерский интерфейс, TMS-интеграции.
- Автоперепланирование от текущей GPS-позиции водителя (фаза 3: кнопка «Перепланировать» считает от старта с тем же входом).
- `expo-maps` вместо `react-native-maps` (alpha, не работает в Expo Go); ключ Google Maps для публикации на Android.
- Проекция парковки на отрезок полилинии (сейчас `routeProgressM` — прогресс ближайшей вершины).
- Параллельные запросы коридора (сейчас 36 запросов подряд; мок в процессе, это миллисекунды).
- `at` запроса коридора, точно совпадающий с `etaAt` парковки (сейчас расхождение до ~9 мин = радиус коридора / скорость).
- Радиус коридора и шаг обхода как настройка пользователя (сейчас параметры функции с дефолтами 15 / 25 км).
- Переключение `EXPO_PUBLIC_TOMTOM_LIVE` на лету (сейчас читается один раз при старте приложения).
- Реальный API парковок при `EXPO_PUBLIC_TOMTOM_LIVE=1` (opt-in касается только TomTom; парковки мокаются всегда).
- Мок-сценарий `'live'`: дрейф, зависящий от `at` (прогноз), а не от `now` (сейчас — только от текущего времени).
- Сохранение поездок, аккаунты, пуши.
- Мок TomTom: реальная геометрия дорог, переменная скорость, трафик/задержки в генераторе (сейчас прямые между локациями и 100 км/ч).
- Мок парковок: местное время суток вместо UTC, занятость по конкретному часу, реальные названия/позиции стоянок вдоль дорог (сейчас: день/ночь по UTC, детерминированная сетка 0.3° с 1–2 парковками в ячейке, имена `Mock parking XXXX-n`).
- Показания тахографа на входе `planTrip` (вождение с последнего перерыва / за день); сейчас водитель считается выехавшим после отдыха.
- Последовательное якорение: следующий якорь от выбранной парковки, выбор парковки по времени вождения до неё вместо расстояния до якоря (сейчас: все якоря из вызова 1, ближайшая по haversine, защитная проверка плеч ≤ 4:30 → conflict).
- Реальное время отъезда к парковке через TomTom по каждому кандидату (сейчас: не считается).
- Радиус поиска парковок и порог сдвига 30 мин как входные параметры `planTrip` (сейчас: константы 25 км / 50 км / 1800 с).
